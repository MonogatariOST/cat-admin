# Default environment variables (shared across all environments) / 默认环境变量（所有环境共享）

## API / 接口地址
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT=10000

## App / 应用配置
VITE_APP_TITLE=CatAdmin

## Auth / 认证
VITE_AUTH_TOKEN_KEY=auth_token
