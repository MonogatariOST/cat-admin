/**
 * Teacher API type definitions / 老师 API 类型定义
 */

/** Teacher data / 老师数据 */
export interface Teacher {
  id: string;
  name: string;
  email: string;
  phone: string;
  department: string;
  title: string;
  status: string;
}
