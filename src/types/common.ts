/** User role literal type / 用户角色字面量类型 */
export type UserRole = "admin" | "editor" | "member";

/** Account status literal type / 账号状态字面量类型 */
export type UserStatus = "active" | "inactive" | "suspended";
